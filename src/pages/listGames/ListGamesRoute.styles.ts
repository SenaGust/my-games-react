import { createStyles, makeStyles } from "@material-ui/core";

const useListGamesRouteStyles = makeStyles(() =>
  createStyles({
    svgIcon: {
      width: 32,
      height: 32,
      fill: "black",
    },
  })
);

export default useListGamesRouteStyles;
